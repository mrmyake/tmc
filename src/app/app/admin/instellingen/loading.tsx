import { PageSkeleton } from "@/app/app/_components/PageSkeleton";

export default function InstellingenLoading() {
  return (
    <PageSkeleton eyebrow="Admin cockpit" title="Instellingen." rows={6} />
  );
}
